This directory contains samples of configuration files for the
programs used in the servers, such as nginx and others.

- nginx/default
You only have to copy it to /etc/nginx/sites-availables/ and change the variable "$my_user" to your user.
You can execute locally the 'whoami' command to know which is your user.
