from django.db import models

# Create your models here.

"""
Control Panell app does not need to have its own db (at the present moment) its just uses the W provided by the modules to
allow the user to interact with the system

"""
